# @summonghost/context-documents

Pure helpers for Markdown documents used as AI context. The package has no
storage, provider, authentication, or UI dependency.

It provides Markdown normalization and metadata extraction, bounded catalog
and full-document rendering, and revisioned-draft reconciliation. Consumers
own persistence, authorization, audience rules, and save orchestration.
