# @summonghost/markdown-editor-react

A small, lazy-loadable React Markdown editor built on Milkdown. Import the
scoped stylesheet separately and customize it with `--ghostkit-editor-*` CSS
variables. Persistence, autosave, conflicts, and product controls stay in the
consumer.
