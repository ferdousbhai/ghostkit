# Changesets

Add a changeset for every consumer-visible contract change:

```sh
pnpm changeset
```

The package version is part of the adoption contract. Consumers must pin an
exact published version before deployment.
